const prompt = require('prompt-sync')();
const arr = prompt('Enter an array: ');
const last = arr[arr.length - 1];
array = JSON.parse(arr);
// array = [10, 20, 30, 40];
// const final = array[array.length -1];


console.log(last);
// console.log(final);